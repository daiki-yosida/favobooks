<% if logged_in? %>
  <%= render 'books/books', books: @books %>
<% else %>
<div class= "mx-auto mw-100 bg-warning text-center">
  <h2>よこうそ</h2>
  <h1>FavoBooksへ</h1>
  <div>
    <%= link_to "ログイン", login_path %>
    <%= link_to "新規登録", signup_path %>
  </div>
</div>
<% end %>